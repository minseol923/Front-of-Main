<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="com.Finally.dao.impl.SignupDAO">
    <insert id="insertSignupVO">
        insert into member values(
		        #{mem_id},
				#{mem_pw}, 
				#{mem_name},
				#{mem_birth}, 
				#{mem_email}, 
				#{mem_phone}, 
				#{mem_gender}
				 )
    </insert>
   <!--  <update id="updateSignupVO" parameterType="java.util.HashMap">
        update member set
        mem_pw = #{mem_pw},mem_name = #{mem_name},
        mem_birth = #{mem_birth}, mem_email = #{mem_email}, 
        mem_phone= #{mem_phone}, mem_gender={mem_gender}
        where mem_id = #{mem_id}
    </update>
    <delete id="deleteSignupVO" parameterType="String">
        delete from member where mem_id = #{mem_id}
    </delete>
    <select id="selectOne" parameterType="String" resultType="java.util.HashMap">
        select * from member where mem_id = #{mem_id}
    </select>
    <select id="selectAll" resultType="java.util.HashMap">
        select * from member
    </select> -->
</mapper>

